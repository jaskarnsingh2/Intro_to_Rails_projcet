<!DOCTYPE html>
<html>
<head>
  <title>My Book Store</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <!-- Include application.scss through the asset pipeline (Bootstrap included here) -->
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

  <!-- Removed the CDN link for Bootstrap -->
  <!-- <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css' %> -->

  <%= javascript_importmap_tags %>
</head>

<body>
  <div class="container">
    <header class="py-3 mb-4 border-bottom">
      <div class="container d-flex justify-content-between">
        <h1 class="mb-0">My Book Store</h1>
        <nav>
          <ul class="list-inline">
            <li class="list-inline-item"><%= link_to 'Home', root_path %></li>
            <li class="list-inline-item"><%= link_to 'Books', books_path %></li>
            <li class="list-inline-item"><%= link_to 'Magazines', magazines_path %></li>
            <li class="list-inline-item"><%= link_to 'About', about_path %></li>
          </ul>
        </nav>
      </div>
    </header>

    <main class="mb-4">
      <%= yield %>
    </main>

    <footer class="py-3 border-top">
      <div class="container">
        <p class="mb-0">Location: Winnipeg, Manitoba, Canada</p>
      </div>
    </footer>
  </div>
</body>
</html>
